/**
 * 视口边缘缩进安全边距
 */
export type IPaddingConfig = {
    /** 顶边距 (默认 30) */
    top?: number;
    /** 底边距 (默认 30) */
    bottom?: number;
    /** 左边距 (默认 40) */
    left?: number;
    /** 右边距 (默认 40) */
    right?: number;
};
